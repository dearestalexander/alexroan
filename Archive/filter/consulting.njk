---
title: Business and technology transformation
titletwo: ~ by Alex Roan
layout: 'blogroll.njk'
main__class: main-consulting
---

<ul role="list" class="blog-grid">

{%- for consulting in collections.consulting | reverse -%}
  <li class="flow blog-card">
    <article class="flow" aria-labelledby="{{consulting.data.title}}">
      <h3 id="{{consulting.data.title}}">{{ consulting.data.title }}</h3>
      <img src="{{ consulting.data.image }}" alt=""/>
      <p>by <span>{{ consulting.data.author }}</span> on <span>{{ consulting.data.date | postDate }}</span></p>
      <p>{{ consulting.data.description }}</p>
      <a href="{{ consulting.url }}">Continue reading</a>
    </article>
  </li>
{%- endfor -%}